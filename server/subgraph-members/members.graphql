extend schema
    @link(url: "https://specs.apollo.dev/federation/v2.0",
    import: ["@key"])
    type Query {
        Member: [Member]
    }

    type Member @key(fields: "locationId") {
        id: ID!
        name: String!
        locationId: ID!
    }